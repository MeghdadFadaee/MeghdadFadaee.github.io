{
  "name": "meghdad-fadaee-portfolio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs && tailwindcss -i ./src/styles.css -o ./dist/assets/site.css --minify",
    "check": "node scripts/check.mjs",
    "test": "node --test scripts/*.test.mjs",
    "verify": "npm run build && npm test && npm run check"
  },
  "devDependencies": {
    "tailwindcss": "3.4.17"
  },
  "engines": {
    "node": ">=22"
  }
}
